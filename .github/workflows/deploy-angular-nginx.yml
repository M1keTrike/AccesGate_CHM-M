name: Deploy Angular to Nginx Container on EC2

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd Access-Gate-APP/Access-Gate
          npm ci

      - name: Build Angular app
        run: |
          cd Access-Gate-APP/Access-Gate
          npm run build -- --configuration=production

      - name: Verify Build Directory
        run: |
          echo "🔍 Verificando estructura del build..."
          ls -R Access-Gate-APP/Access-Gate/dist/

      - name: Remove old files on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            if [ -d "/var/www/angular-app" ]; then
              sudo rm -rf /var/www/angular-app/*
              echo "✅ Archivos antiguos eliminados"
            else
              echo "⚠️ El directorio no existe. Creando..."
              sudo mkdir -p /var/www/angular-app
            fi

      # 🔽 🔥 **Corrección de Upload a EC2**
      - name: Upload dist folder to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          source: "Access-Gate-APP/Access-Gate/dist/access-gate/browser/*"
          target: "/var/www/angular-app/"
          strip_components: 3  # 🔥 Evita enviar directorios intermedios innecesarios

      - name: Check Deployment Files on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            if [ ! -f "/var/www/angular-app/index.html" ]; then
              echo "❌ ERROR: index.html no encontrado en /var/www/angular-app"
              exit 1
            fi
            echo "✅ index.html encontrado. Todo listo para reiniciar Nginx."

      - name: Fix Permissions
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            sudo chmod -R 755 /var/www/angular-app
            sudo chown -R www-data:www-data /var/www/angular-app
            echo "✅ Permisos corregidos"

      - name: Restart Nginx Container
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
          script: |
            docker restart angular-nginx
            echo "✅ Nginx reiniciado"
